## Порядок запуска

1. Создание БД
   `python backend/manage.py migrate`
2. Запуск сервера
   `python backend/manage.py runserver`
3. Запуск форнтенда
   `cd frontend`
   `npm install`
   `npm start`
